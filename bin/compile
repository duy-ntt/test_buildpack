#!/usr/bin/env bash

# Fail fast and fail hard.
set -eo pipefail

echo "source /app/.heroku/python/bin/virtualenvwrapper.sh" >> ~/.bashrc

source ~/.bashrc

mkvirtualenv kaleido_admin -p python3

echo "<><><><><><>"